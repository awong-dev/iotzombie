'use strict';

const assert = require(`assert`);
const lights = require(`../../routes/lights`);

describe('lights route', () => {
  it(`should create an express app`, (done) => {
    done()
  });
});
